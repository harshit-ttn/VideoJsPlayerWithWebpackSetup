import React from "react";

interface ShakaPlayerProps {
  options: any;
  sources: any;
}

const ShakaPlayer: React.FC<ShakaPlayerProps> = ({ options, sources }) => {
  return <div>shakaPlayer</div>;
};

export default ShakaPlayer;
